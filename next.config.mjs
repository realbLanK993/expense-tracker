/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/expense-tracker", // Replace with your GitHub repository name
  assetPrefix: "/expense-tracker/",
};;

export default nextConfig;
